# 🚀 Быстрый старт проекта

## ✅ Проверка готовности

### 1. Backend готов:
- ✅ Все зависимости установлены
- ✅ API ключи настроены
- ✅ Qdrant подключен
- ✅ База данных будет создана автоматически

### 2. Frontend:
- ⚠️ Нужно проверить установку зависимостей

---

## 📋 Пошаговая инструкция запуска

### Шаг 1: Запуск Backend

```bash
cd backend
source .venv/bin/activate  # или .venv\Scripts\activate на Windows
uvicorn app.main:app --reload --port 8000
```

**Проверка:** Откройте http://localhost:8000/docs

### Шаг 2: Установка зависимостей Frontend (если еще не установлены)

```bash
cd frontend
npm install
```

### Шаг 3: Запуск Frontend

```bash
cd frontend
npm run dev
```

**Проверка:** Откройте http://localhost:5173

---

## 🧪 Тестирование функционала

### 1. Проверка API

```bash
# Health check
curl http://localhost:8000/api/v1/health/

# Должен вернуть: {"status":"ok","service":"memorial-mvp"}
```

### 2. Создание первого мемориала

Через Swagger UI (http://localhost:8000/docs):
1. Откройте `POST /api/v1/memorials/`
2. Нажмите "Try it out"
3. Вставьте JSON:
```json
{
  "name": "Тестовый мемориал",
  "description": "Это тестовый мемориал для проверки",
  "birth_date": "1950-01-01T00:00:00Z",
  "death_date": "2020-01-01T00:00:00Z"
}
```
4. Нажмите "Execute"
5. Сохраните `id` из ответа

### 3. Добавление воспоминания

1. Откройте `POST /api/v1/memorials/{id}/memories`
2. Вставьте JSON:
```json
{
  "title": "Первое воспоминание",
  "content": "Это было замечательное время. Мы часто ходили в парк и играли в футбол."
}
```
3. Нажмите "Execute"
4. Embedding будет создан автоматически в фоне

### 4. Тест чата с аватаром

1. Откройте `POST /api/v1/ai/avatar/chat`
2. Вставьте JSON:
```json
{
  "memorial_id": 1,
  "question": "Что любил делать?"
}
```
3. Нажмите "Execute"
4. Должен вернуться ответ на основе воспоминаний

### 5. Загрузка фото

1. Откройте `POST /api/v1/memorials/{id}/media/upload`
2. Выберите файл (фото)
3. Нажмите "Execute"
4. Фото будет сохранено локально в `backend/uploads/`

---

## 🎯 Основные URL

- **Backend API:** http://localhost:8000
- **API Docs (Swagger):** http://localhost:8000/docs
- **Frontend:** http://localhost:5173
- **Qdrant Dashboard:** https://cloud.qdrant.io/ (если используете Qdrant Cloud)

---

## ⚠️ Возможные проблемы

### Backend не запускается

1. **Проверьте виртуальное окружение:**
```bash
cd backend
source .venv/bin/activate
pip install -r requirements.txt
```

2. **Проверьте .env файл:**
```bash
cat backend/.env | grep -E "OPENAI|QDRANT|HEYGEN"
```

3. **Проверьте порт 8000:**
```bash
lsof -i :8000  # Mac/Linux
netstat -ano | findstr :8000  # Windows
```

### Frontend не запускается

1. **Установите зависимости:**
```bash
cd frontend
npm install
```

2. **Проверьте порт 5173:**
```bash
lsof -i :5173  # Mac/Linux
```

### Ошибки подключения к Qdrant

1. **Проверьте URL и API Key в .env**
2. **Проверьте подключение:**
```bash
curl -H "api-key: YOUR_API_KEY" \
  https://YOUR-QDRANT-URL/collections
```

### Ошибки OpenAI

1. **Проверьте API Key:**
```bash
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer YOUR_OPENAI_KEY"
```

---

## 📝 Следующие шаги после запуска

1. ✅ Создайте первый мемориал
2. ✅ Добавьте несколько воспоминаний
3. ✅ Загрузите фото
4. ✅ Протестируйте чат с аватаром
5. ✅ Попробуйте анимацию фото (требует worker)

---

## 🔧 Запуск Worker (для фоновых задач)

Для анимации фото и создания embeddings нужен Celery worker:

```bash
cd backend
source .venv/bin/activate
celery -A app.workers.worker worker --loglevel=info
```

**Примечание:** Для MVP можно обойтись без worker, embeddings создаются синхронно.

---

## ✅ Готово!

Проект готов к использованию. Если возникнут проблемы, проверьте логи в терминале.
