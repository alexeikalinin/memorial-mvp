FROM node:18
WORKDIR /app
COPY . .
CMD ["node", "../backend/server.js"]
version: '3'
services:
  backend:
    build: ../backend
    ports:
      - "3000:3000"
  frontend:
    build: ../frontend
    ports:
      - "8080:8080"
# Infrastructure
Instructions to run the project locally.
