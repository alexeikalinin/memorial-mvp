# 🚀 Быстрая настройка ngrok для HeyGen

## Проблема
HeyGen не может получить доступ к изображениям по `localhost:8000`. Нужен публичный URL.

## Решение за 3 шага:

### Шаг 1: Авторизация в ngrok (один раз)

1. Зайдите на https://dashboard.ngrok.com/signup (или https://dashboard.ngrok.com/login если уже зарегистрированы)
2. Зарегистрируйтесь/войдите
3. Скопируйте ваш authtoken со страницы https://dashboard.ngrok.com/get-started/your-authtoken
4. Выполните в терминале:
```bash
ngrok config add-authtoken ВАШ_ТОКЕН_ЗДЕСЬ
```

### Шаг 2: Запуск ngrok

В **новом терминале** выполните:
```bash
ngrok http 8000
```

Вы увидите что-то вроде:
```
Forwarding  https://abc123-def456.ngrok-free.app -> http://localhost:8000
```

Скопируйте HTTPS URL (например: `https://abc123-def456.ngrok-free.app`)

### Шаг 3: Добавление в .env

Откройте файл `backend/.env` и добавьте/обновите строку:
```env
PUBLIC_API_URL=https://abc123-def456.ngrok-free.app
```

**Важно:** Замените `https://abc123-def456.ngrok-free.app` на ваш реальный URL из ngrok!

### Готово! ✅

Backend автоматически перезагрузится (если запущен с `--reload`), и анимация фото начнет работать!

---

## Альтернатива: Автоматический скрипт

После авторизации в ngrok, вы можете использовать скрипт:
```bash
./start_ngrok.sh
```

Он автоматически:
- Запустит ngrok
- Получит публичный URL
- Добавит его в `.env`

---

## Проверка работы

1. Убедитесь, что ngrok запущен (в отдельном терминале)
2. Убедитесь, что backend запущен на порту 8000
3. Попробуйте анимировать фото - должно работать!

## Остановка ngrok

Нажмите `Ctrl+C` в терминале, где запущен ngrok, или:
```bash
pkill ngrok
```

---

**Примечание:** Каждый раз при перезапуске ngrok URL может измениться. Нужно будет обновить `PUBLIC_API_URL` в `.env`.

