# Чек-лист для проверки Sprint 0: Backend Skeleton

## ✅ Подготовка окружения

- [ ] Python 3.11+ установлен
- [ ] PostgreSQL установлен (или используется SQLite для dev)
- [ ] Redis установлен (опционально, для Celery)
- [ ] Docker и Docker Compose установлены (опционально)

## ✅ Настройка проекта

- [ ] Репозиторий клонирован
- [ ] Создана ветка `feature/backend-skeleton` (если нужно)
- [ ] Переход в директорию `backend/`
- [ ] Создано виртуальное окружение: `python3 -m venv .venv`
- [ ] Активировано виртуальное окружение: `source .venv/bin/activate`
- [ ] Установлены зависимости: `pip install -r requirements.txt`

## ✅ Конфигурация

- [ ] Скопирован `.env.example` в `.env`
- [ ] Отредактирован `.env` файл:
  - [ ] `DATABASE_URL` настроен (SQLite или PostgreSQL)
  - [ ] `SECRET_KEY` изменен на уникальный
  - [ ] API ключи добавлены (если доступны):
    - [ ] `OPENAI_API_KEY`
    - [ ] `DID_API_KEY`
    - [ ] `ELEVENLABS_API_KEY`
    - [ ] `PINECONE_API_KEY`

## ✅ База данных

- [ ] PostgreSQL запущен (через Docker Compose или локально)
  ```bash
  docker-compose up -d postgres
  ```
- [ ] Или используется SQLite (автоматически создается)

## ✅ Запуск сервера

- [ ] Сервер запущен:
  ```bash
  uvicorn app.main:app --reload --port 8000
  ```
- [ ] Сервер отвечает на http://localhost:8000
- [ ] Документация доступна на http://localhost:8000/docs

## ✅ Проверка API Endpoints

### Health Check
- [ ] `GET /api/v1/health/` возвращает `{"status": "ok", "service": "memorial-mvp"}`

### Создание мемориала
- [ ] `POST /api/v1/memorials/` создает мемориал
  ```bash
  curl -X POST http://localhost:8000/api/v1/memorials/ \
    -H "Content-Type: application/json" \
    -d '{"name": "Тест", "description": "Описание", "is_public": false}'
  ```
- [ ] Возвращается JSON с `id`, `name`, `created_at`

### Получение мемориала
- [ ] `GET /api/v1/memorials/{id}` возвращает мемориал
- [ ] В ответе есть поля: `id`, `name`, `description`, `media`, `memories`

### Загрузка медиа
- [ ] `POST /api/v1/memorials/{id}/media/upload` загружает файл
  ```bash
  curl -X POST http://localhost:8000/api/v1/memorials/1/media/upload \
    -F "file=@/path/to/photo.jpg"
  ```
- [ ] Файл сохраняется в директории `uploads/`
- [ ] В БД создается запись `Media` с правильным `media_type`

### Добавление воспоминания
- [ ] `POST /api/v1/memorials/{id}/memories` создает воспоминание
  ```bash
  curl -X POST http://localhost:8000/api/v1/memorials/1/memories \
    -H "Content-Type: application/json" \
    -d '{"title": "Воспоминание", "content": "Текст..."}'
  ```

### AI Endpoints (требуют API ключи)
- [ ] `POST /api/v1/ai/photo/animate` принимает запрос (может не работать без D-ID ключа)
- [ ] `POST /api/v1/ai/avatar/chat` принимает запрос (может не работать без OpenAI/Pinecone ключей)

## ✅ Проверка структуры БД

- [ ] Таблицы созданы:
  - [ ] `users`
  - [ ] `memorials`
  - [ ] `media`
  - [ ] `memories`
- [ ] Связи между таблицами работают (foreign keys)

## ✅ Тестирование

- [ ] Запуск тестов:
  ```bash
  cd backend
  pytest tests/ -v
  ```
- [ ] Все тесты проходят:
  - [ ] `test_health_check`
  - [ ] `test_create_memorial`
  - [ ] `test_get_memorial`

## ✅ Workers (опционально)

### Celery Worker
- [ ] Redis запущен: `docker-compose up -d redis`
- [ ] Celery worker запущен:
  ```bash
  celery -A app.workers.worker worker --loglevel=info
  ```

### Простой Worker
- [ ] Простой worker запущен:
  ```bash
  python -m app.workers.worker_simple
  ```

## ✅ Проверка файловой структуры

- [ ] Все файлы созданы:
  - [ ] `backend/app/main.py`
  - [ ] `backend/app/config.py`
  - [ ] `backend/app/db.py`
  - [ ] `backend/app/models.py`
  - [ ] `backend/app/schemas.py`
  - [ ] `backend/app/api/health.py`
  - [ ] `backend/app/api/memorials.py`
  - [ ] `backend/app/api/ai.py`
  - [ ] `backend/app/services/ai_tasks.py`
  - [ ] `backend/app/workers/worker.py`
  - [ ] `backend/app/workers/worker_simple.py`
  - [ ] `backend/requirements.txt`
  - [ ] `backend/Dockerfile`
  - [ ] `backend/docker-compose.yml`
  - [ ] `backend/README.md`
  - [ ] `.gitignore`
  - [ ] `README.md`

## ✅ Безопасность

- [ ] `.env` файл в `.gitignore`
- [ ] `.env.example` не содержит реальных ключей
- [ ] `SECRET_KEY` изменен с дефолтного значения

## ✅ Документация

- [ ] `README.md` прочитан и понятен
- [ ] `backend/README.md` содержит инструкции по запуску
- [ ] API документация доступна на `/docs`

## 🎯 Следующие шаги (Sprint 1)

После успешной проверки всех пунктов можно переходить к:
- [ ] Настройка S3 для хранения медиа
- [ ] Генерация миниатюр для изображений
- [ ] Улучшение обработки загрузки файлов
- [ ] Добавление валидации размеров файлов

---

**Примечания:**
- Если какой-то пункт не работает, проверьте логи сервера
- Для тестирования без API ключей можно использовать mock-данные
- В production обязательно используйте PostgreSQL и настройте миграции Alembic

